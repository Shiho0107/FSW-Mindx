/**
 * roles — centralised role constants.
 * Avoid magic strings throughout the codebase.
 */
const ROLES = {
  ADMIN:   "admin",
  TEACHER: "teacher",
  STUDENT: "student",
};

export default ROLES;
